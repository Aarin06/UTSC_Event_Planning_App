package com.example.cscb07_project;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;

import android.content.Context;
import android.content.SharedPreferences;
import android.util.Log;
import android.view.View;

public class RegardAndAddVenues_Admin extends AppCompatActivity {
///okay so ethans should link to mine: view venues
    //use intent to start activity to mine.
    //a two way button between mine and miles so the admin can view venues or by events

    //TODO: recycler view, connect data, button connecting miles, debuggin
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_regard_and_add_venues_admin);
    }

    public void viewVenueEvents (View view){

    }
}
//Images Should be drawn

